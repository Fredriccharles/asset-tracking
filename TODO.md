# Excel Import Feature — Implementation Steps

1. [x] Add `importItems` + `downloadTemplate` to `server/controllers/itemController.js`
2. [x] Add multer + import/template routes to `server/routes/items.js`
3. [x] Add `importItems` + `downloadImportTemplate` to `client/src/api/api.js`
4. [x] Add "Import Excel" button + import modal to `client/src/pages/Items.jsx`
5. [x] Test: client builds, server routes/controller load, exceljs available
